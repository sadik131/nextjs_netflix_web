/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['images.unsplash.com',"i.cdn.newsbytesapp.com","wp.scoopwhoop.com","www.highsnobiety.com"],
    }
};

export default nextConfig;
